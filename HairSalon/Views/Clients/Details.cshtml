@{
  Layout = "_Layout";
}

<div class="parent">

  <div class="home-title">
    <p>Eau Claire's Salon</p>
  </div>
  
</div>

@model HairSalon.Models.Client

<div class="parent">
  <div class="page-content">
    <h2 class="sub-title">Client Details</h2>
    <hr />
    <p class="bigger-info">@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</p>
    <p class="link">@Html.ActionLink("Edit Client", "Edit", new { id = Model.ClientId })</p>
    <p class="link">@Html.ActionLink("Delete Client", "Delete", new { id = Model.ClientId })</p>
    <p class="link">@Html.ActionLink("Back to client list", "Index")</p>
  </div>
</div>

